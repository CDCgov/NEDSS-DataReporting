SELECT
    LDF_UID,
    INVESTIGATION_UID,
    BUSINESS_OBJECT_NM,
    CONDITION_CD,
    DATA_TYPE,
    PHC_CD,
    COL1
FROM RDB_MODERN.dbo.LDF_DIMENSIONAL_DATA
WHERE INVESTIGATION_UID = 97000200
  AND LDF_UID IS NOT NULL
ORDER BY LDF_UID;
