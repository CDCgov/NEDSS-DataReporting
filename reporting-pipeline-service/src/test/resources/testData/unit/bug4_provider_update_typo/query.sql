SELECT
    PROVIDER_UID,
    PROVIDER_LAST_NAME,
    PROVIDER_FIRST_NAME,
    PROVIDER_CITY,
    PROVIDER_STATE,
    PROVIDER_ZIP,
    PROVIDER_QUICK_CODE
FROM RDB_MODERN.dbo.D_PROVIDER
WHERE PROVIDER_UID = 49000004;
