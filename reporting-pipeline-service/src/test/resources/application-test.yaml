spring:
  datasource:
    default:
      url: ${DB_CONNECTION_URL:jdbc:sqlserver://localhost:3433;databaseName=RDB_MODERN;encrypt=true;trustServerCertificate=true;loginTimeout=3;}
      username: ${RTR_SERVICE_USER_NAME:rtr_service_user}
      password: ${RTR_SERVICE_USER_PASSWORD:rtr_service_user}
    admin:
      url: ${DB_CONNECTION_URL:jdbc:sqlserver://localhost:3433;databaseName=RDB_MODERN;encrypt=true;trustServerCertificate=true;loginTimeout=3;}
      username: ${RTR_ADMIN_NAME:rtr_admin}
      password: ${RTR_ADMIN_PASSWORD:rtr_admin}
  jpa:
    show-sql: "true"
    hibernate:
      ddl-auto: none
  kafka:
    producer:
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    streams:
      application-id: reporting-pipeline-service
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVER:localhost:9092}
    consumer:
      maxPollIntervalMs: 3000
      metadata.max.age.ms: 6000
  liquibase:
    enabled: true
    user: ${RTR_ADMIN_NAME:rtr_admin}
    password: ${RTR_ADMIN_PASSWORD:rtr_admin}

connector:
  database:
    url: "jdbc:sqlserver://nbs-mssql:1433;databaseName=RDB_MODERN;encrypt=true;trustServerCertificate=true;loginTimeout=3"
    host: nbs-mssql
    port: "1433"
    username: ${RTR_ADMIN_NAME:rtr_admin}
    password: ${RTR_ADMIN_PASSWORD:rtr_admin}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVER:kafka:9092}
  debezium:
    enabled: true
    url: http://localhost:8085
  kafka-connect:
    enabled: true
    url: http://localhost:8083
