# Comparison Fixtures (APP-471)

Synthetic NBS_ODSE INSERTs designed to maximize RDB_MODERN coverage when RTR
runs initial hydration. Output is consumed by a planned RDB-vs-RDB_MODERN
comparison test against MasterETL.

## Status — 2026-05-27 (live-verified, clean from-scratch run)

End-to-end `scripts/merge_and_verify.sh` against 118 in-scope RDB_MODERN
target tables, then `scripts/coverage_summary.sh`:

- **Fully covered: 76** (every column populated for at least one row)
- **Partially covered: 36**
- **Empty: 5**
- **Missing from live schema: 1** (`job_batch_rebuild_log` — not in baseline 6.0.18.1)
- **Overall column coverage: 89.9%** (4165 / 4633 columns, live-verified)
- **14 RTR bugs surfaced and documented** (numbered #1–#13 and #15;
  3 fixes merged upstream, 6 fixed on this branch, 5 documented with
  repros for follow-up — bugs #9 and #10 LANDED this branch in commits
  `a88e40e5` and `99ef3517`)

Wall-clock for the full pipeline: ~5 min from `docker compose down -v`.
Zero external dependencies beyond the baseline image.

This is a real, deterministic from-scratch number — not an estimate.
The earlier disk-full block (2026-05-25 ~00:30 PDT) is **resolved**:
host disk was freed, the DB rebuilt from a fresh baseline, and the
pipeline now runs clean end-to-end (see `BLOCKED.md`). The 89.9%
exceeds the prior 84.4% and the 85% target **even with**
`zz_hepatitis_datamart_round2.sql` quarantined — its SP chain
(`sp_hepatitis_datamart_postprocessing`) spills ~70GB into tempdb on a
cold full dataset and was the recurring disk-full culprit. Restoring
that fixture (~+61 hep cols) is the one outstanding follow-up; it needs
an SP fix or a tempdb cap first. Two clean-rebuild bugs were fixed in
passing (hep100 FK/ordering, lab100 OID-into-bigint) — see commit
`Fix clean-rebuild blockers`.

## Documents

| Path | Purpose |
| ---  | ---     |
| [`STRATEGY.md`](./STRATEGY.md) | Strategy, baseline, build order, agent contracts, coverage report schema. The load-bearing source of truth — referenced by every fixture and sub-agent. |
| [`METHODOLOGY.md`](./METHODOLOGY.md) | ~1000-word writeup of *how* we built this — reverse-engineering from RDB_MODERN write targets back through the SP chain to ODSE inputs. Calibrated for an NBS/RTR-fluent outside reader. |
| [`DEMO.md`](./DEMO.md) | Live demo cheat-sheet — 5-minute walkthrough with terminal commands, talking points, anticipated questions. |
| [`bugs/README.md`](./bugs/README.md) | RTR bugs surfaced during fixture authoring, with repros and fix status. |
| [`catalog/rtr_target_columns.md`](./catalog/rtr_target_columns.md) | Phase 0 — static-extracted (table, column) pairs RTR writes. |
| [`catalog/edge_types.md`](./catalog/edge_types.md) | Phase B — legal `type_cd` values for connective tables, grounded in baseline SRTE. |
| [`catalog/uid_ranges.md`](./catalog/uid_ranges.md) | UID range registry — every agent's allocation. |
| [`catalog/odse_unknown_tables.md`](./catalog/odse_unknown_tables.md) | Classification of the ~80 RDB tables nobody knew how to populate — 22 MasterETL-only, 38 datamart-SP-driven, 34 Tier-1/2 reachable. |
| [`coverage/coverage_merged.md`](./coverage/coverage_merged.md) | Canonical headline coverage report, regenerated by `scripts/coverage_summary.sh`. |
| `coverage/coverage_<subject>.md` | Per-fixture coverage report (one per Tier 1/2/3 fixture). |

## Directory layout

```
utilities/comparison-fixtures/
├── STRATEGY.md, METHODOLOGY.md, DEMO.md, README.md   (this file)
├── bugs/                       RTR bug investigations (10 dirs + README)
├── catalog/                    Reference data (target columns, edges, UIDs, classifications)
├── coverage/                   One markdown report per fixture + the merged rollup
├── fixtures/
│   ├── 00_foundation/          Tier 0 — canonical instance of each subject
│   ├── 10_subjects/            Tier 1 — one fixture per subject (11 subjects)
│   ├── 20_links/               Tier 2 — one fixture per cross-subject edge type
│   ├── 30_sp_coverage/         Tier 3 — gap-driven SP coverage + Phase-2 chains
│   │   └── _quarantine/        (empty — past failed fixtures kept here)
├── prompts/                    Sub-agent prompts and templates
└── scripts/
    ├── merge_and_verify.sh     End-to-end Merge contract orchestrator
    ├── coverage_summary.sh     Regenerate coverage_merged.md
    └── db_lock.sh              Parallel-agent DB lock (mkdir-based atomic)
```

## Build order

See `STRATEGY.md` → "Build order". Summary:

1. **Phase 0** (`prompts/phase_0_target_columns.md`) → `catalog/rtr_target_columns.md`
2. **Phase B** (`prompts/phase_b_edge_catalog.md`) → `catalog/edge_types.md`
3. **Tier 0** (`prompts/tier_0_foundation.md`) → `fixtures/00_foundation/`
4. **Provider canary** (`prompts/tier_1_provider_canary.md`) → derives Tier 1 template
5. **Tier 1 batch** — per-subject (~11 agents)
6. **Tier 2** — cross-subject links (one agent per edge type)
7. **Tier 3** — gap-driven SP coverage (Phase-2 full-chain fixtures: TB, COVID, Var, STD/HIV, BMIRD, d_investigation_repeat)
8. **Final merge + verification** — `scripts/merge_and_verify.sh`

## Running the pipeline

```sh
cd utilities/comparison-fixtures
./scripts/merge_and_verify.sh     # ~5 min, runs the full 9-step Merge contract
./scripts/coverage_summary.sh     # Regenerates coverage_merged.md
head -12 coverage/coverage_merged.md   # Headline numbers
```

## What's next

1. **Restore `zz_hepatitis_datamart_round2.sql`** (currently quarantined
   for the ~70GB tempdb blowup — see `BLOCKED.md` and
   `fixtures/30_sp_coverage/_quarantine/README.md`). Needs an SP fix to
   `sp_hepatitis_datamart_postprocessing` / `sp_f_page_case_postprocessing`
   or a tempdb MAX_SIZE cap before it can safely re-enter the cold
   single-batch pipeline. Recovers Agent Q's ~+61 hep cols (would push
   the headline past 90%).
2. **Investigate the open bugs #11, #12, #13, #15** (aggregate_report
   schema mismatch, BMIRD ROW_NUMBER PARTITION, sld_investigation_repeat
   TEXT-pivot NULL propagation, event_metric/SR100 ADD_USER_NAME NULL).
   Bug #13's fix alone would add ~50 TEXT cols on D_INVESTIGATION_REPEAT
   for free. See `bugs/README.md` for repros and suggested fixes.
3. **MasterETL-side coverage analysis** — mirror Phase 0 against
   MasterETL SPs in `NEDSSDB/src/migrations/*/RDB/`. Output:
   `catalog/masteretl_target_columns.md`. Without this, the eventual
   diff tool can't distinguish "fixture didn't seed it" from
   "MasterETL also doesn't write it" from "RTR gap".
4. **The comparison tool itself** — the actual diff harness, modeled
   on NEDSS-DataCompare. Without this, today's coverage state has
   nowhere to go.
5. **Remaining condition families** for completeness — Pertussis,
   Measles, Rubella full chains. Lower yield now that the baseline
   is at ~90%.

**Earlier "40-45% ceiling" estimate (commits before 2026-05-22) was
blown away.** Two parallel-agent loops plus clean-rebuild fixes took
live coverage from 41.4% to 89.9% verified (full from-scratch run,
2026-05-27). The methodology that drove this:
- Identify biggest table-level gaps via `coverage_summary.sh`
- Spawn 4-5 parallel agents in worktrees, each on a distinct
  datamart with a reserved UID block
- Reconcile via cherry-pick + DB lock + foreground apply
- Document SP-level bugs as `bugs/NN_*/findings.md` rather than
  fixing in-loop

See `LOOP.md` for full loop protocol, `LOOP_round1.md` for the
single-agent overnight precedent.
