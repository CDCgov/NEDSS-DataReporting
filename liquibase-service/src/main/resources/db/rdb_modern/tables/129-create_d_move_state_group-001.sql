IF NOT EXISTS (SELECT 1
               FROM sysobjects
               WHERE name = 'D_MOVE_STATE_GROUP'
                 and xtype = 'U')

BEGIN				 
	CREATE TABLE DBO.D_MOVE_STATE_GROUP (
		D_MOVE_STATE_GROUP_KEY BIGINT NOT NULL ,
		CONSTRAINT PK_D_MOVE_STATE_GROUP PRIMARY KEY CLUSTERED 
		(
			D_MOVE_STATE_GROUP_KEY
		)  ON [PRIMARY ]
	) ON [PRIMARY];
END;

IF NOT EXISTS ( SELECT 1 FROM DBO.D_MOVE_STATE_GROUP WHERE D_MOVE_STATE_GROUP_KEY = 1 )
BEGIN
	INSERT INTO DBO.D_MOVE_STATE_GROUP (D_MOVE_STATE_GROUP_KEY) VALUES (1);
END;